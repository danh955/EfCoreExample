# Simple EF code-first SQLite example

This is a simple example in using the code-first EntityFrameworkCore.  This uses SQLite for the database.  It only uses two NuGet packages.

- Microsoft.EntityFrameworkCore.Sqlite
- Microsoft.EntityFrameworkCore.Tools

It will automatically create database when ran.

